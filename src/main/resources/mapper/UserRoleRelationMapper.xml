<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.base.auth.mapper.UserRoleRelationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.base.auth.entity.UserRoleRelation">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="role_id" property="roleId" />
        <result column="create_time" property="createTime" />
        <result column="create_user_id" property="createUserId" />
        <result column="create_user_name" property="createUserName" />
        <result column="update_time" property="updateTime" />
        <result column="update_user_id" property="updateUserId" />
        <result column="update_user_name" property="updateUserName" />
        <result column="del_flag" property="delFlag" />
    </resultMap>
    <select id="findUserRoleRelationDepartment" resultType="com.base.auth.to.UserRoleDepartmentDto">
        select urr.role_id roleId,ud.department_id departmentId,sa.id authId from sys_auth sa
         left join auth.user_role_relation urr on sa.id = urr.user_id
         left join auth.user_department ud on sa.id = ud.user_id and ud.del_flag = 0
        where sa.del_flag = 0 and urr.del_flag = 0
          and sa.id = #{id}
    </select>

</mapper>
